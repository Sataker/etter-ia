---
import { getCollection, render } from 'astro:content';
import Base from '../../../layouts/Base.astro';
import ServiceArticle from '../../../components/ServiceArticle.astro';
import { faqSchema, serviceSchema, articleSchema, caseSchema } from '../../../lib/schema';
import { LABEL, SEG } from '../../../lib/routes';

export async function getStaticPaths() {
  const entries = await getCollection('services', (e) => e.data.lang === 'en');
  return entries.map((entry) => ({ params: { slug: entry.data.slug }, props: { entry } }));
}

const { entry } = Astro.props;
const lang = 'en' as const;
const { Content } = await render(entry);
const d = entry.data;

const extra = [
  'services' === 'services' ? serviceSchema(d) : null,
  'services' === 'cases' ? caseSchema(d) : null,
  'services' === 'guides' || 'services' === 'industries' ? articleSchema(d, lang, Astro.url.href) : null,
  faqSchema(d.faq),
].filter(Boolean) as Record<string, unknown>[];

const relatedCase =
  'services' === 'industries' && d.caseRef
    ? (await getCollection('cases', (e) => e.data.lang === lang && e.data.slug === d.caseRef))[0]
    : undefined;
---

<Base title={d.seoTitle} description={d.description} updated={d.updated} schema={extra}
  breadcrumbs={[{ name: LABEL[lang].services, path: SEG[lang].services }, { name: d.title, path: `${SEG[lang].services}/${d.slug}` }]}>
  <ServiceArticle lang={lang} entry={entry} relatedCase={relatedCase}>
    <Content />
  </ServiceArticle>
</Base>
