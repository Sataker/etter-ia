---
import { getCollection, render } from 'astro:content';
import Base from '../../../layouts/Base.astro';
import GuideArticle from '../../../components/GuideArticle.astro';
import { faqSchema, serviceSchema, articleSchema, caseSchema } from '../../../lib/schema';
import { LABEL, SEG } from '../../../lib/routes';

export async function getStaticPaths() {
  const entries = await getCollection('guides', (e) => e.data.lang === 'en');
  return entries.map((entry) => ({ params: { slug: entry.data.slug }, props: { entry } }));
}

const { entry } = Astro.props;
const lang = 'en' as const;
const { Content } = await render(entry);
const d = entry.data;

const extra = [
  'guides' === 'services' ? serviceSchema(d) : null,
  'guides' === 'cases' ? caseSchema(d) : null,
  'guides' === 'guides' || 'guides' === 'industries' ? articleSchema(d, lang, Astro.url.href) : null,
  faqSchema(d.faq),
].filter(Boolean) as Record<string, unknown>[];

const relatedCase =
  'guides' === 'industries' && d.caseRef
    ? (await getCollection('cases', (e) => e.data.lang === lang && e.data.slug === d.caseRef))[0]
    : undefined;
---

<Base title={d.seoTitle} description={d.description} updated={d.updated} schema={extra}
  breadcrumbs={[{ name: LABEL[lang].guides, path: SEG[lang].guides }, { name: d.title, path: `${SEG[lang].guides}/${d.slug}` }]}>
  <GuideArticle lang={lang} entry={entry} relatedCase={relatedCase}>
    <Content />
  </GuideArticle>
</Base>
